import React, { useEffect , useRef} from 'react';
import { connect } from 'react-redux';
import Axios from 'axios';
import { updateProductionDate } from '../../../actions/FlavorActions';

const DateProduction = (props) => {
    
    return (
        /*
        <div className="form-row">
            <div className="form-control-label">
                <label className="col-3">Fecha</label>
            </div>
            <div className="row justify-content-end">
                <DatePicker selected={fecha} onChange={onChangeDate} dateFormat="dd/MM/yyyy"/>
            </div>
        </div>
        */
       <h1>Hola</h1>
    )
    
}

export default (DateProduction);